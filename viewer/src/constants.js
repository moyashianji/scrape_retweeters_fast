// グループ定義
export const GROUPS = {
  amptakcolors: {
    name: 'amptak colors',
    fanMark: '📣',
    officialAccount: 'amptakcolors',
    members: {
      'ぷりっつ': {
        mentions: ['ampxtak', 'umasugi_human', 'hinekureo_dayo', 'puri_dao'],
        keywords: ['ぷりっつ', 'プリッツ', 'ぷり民', 'priっつ'],
        fanMark: '🍅',
        color: '#22c55e',
      },
      'あっきぃ': {
        mentions: ['akkkkiy', 'akkkkiysab', 'akkiydaaa'],
        keywords: ['あっきぃ', 'アッキー', 'あっきー', 'akkiy', 'あきぃ'],
        fanMark: '🥔',
        color: '#f97316',
      },
      'あっとくん': {
        mentions: ['_AtToKun', 'AtToKun_info'],
        keywords: ['あっとくん', 'あっと君', 'アットくん', 'あっと民', 'atto'],
        fanMark: '🍷',
        color: '#ef4444',
      },
      'ちぐさくん': {
        mentions: ['Tigusa_voice', 'Tigusa_sub'],
        keywords: ['ちぐさ', 'チグサ', 'tigusa', '千草'],
        fanMark: '🐧',
        color: '#3b82f6',
      },
      'まぜ太': {
        mentions: ['mazeta_666', 'mazeta_sub'],
        keywords: ['まぜ太', 'まぜた', 'マゼタ', 'mazeta'],
        fanMark: '😈',
        color: '#a855f7',
      },
      'けちゃ': {
        mentions: ['ketchup_N1', 'ketchup_N2'],
        keywords: ['けちゃ', 'ケチャ', 'ketchup', 'けちゃっぷ'],
        fanMark: '🐷',
        color: '#ec4899',
      },
    },
  },
  sneakerstep: {
    name: 'Sneaker Step',
    fanMark: '👟',
    officialAccount: 'Sneaker_Step',
    members: {
      'やなと': {
        mentions: ['yanato_desu', 'yanato_sub', 'yanato_babu'],
        keywords: ['やなと', 'ヤナト', 'yanato'],
        fanMark: '☀',
        color: '#eab308',
      },
      'おさでい': {
        mentions: ['osusi0303', 'sabudei0303'],
        keywords: ['おさでい', 'オサデイ', 'osadei'],
        fanMark: '🍄',
        color: '#06b6d4',
      },
      'たちばな': {
        mentions: ['tcbnwst', 'tcbn0404', 'tcbnwst_ura', 'tcbnwst_info'],
        keywords: ['たちばな', 'タチバナ', 'tcbn'],
        fanMark: '🦋',
        color: '#3b82f6',
      },
      'らお': {
        mentions: ['_Raokun_', 'Raokun_sub', '_Raochan_'],
        keywords: ['らお', 'ラオ', 'raokun'],
        fanMark: '🦁',
        color: '#ef4444',
      },
      'だいきり': {
        mentions: ['daiquiri_ch', 'daisubkiri2', 'daisubkiri3'],
        keywords: ['だいきり', 'ダイキリ', 'daiquiri', 'daikiり'],
        fanMark: '🍸',
        color: '#eab308',
      },
      'ゆた': {
        mentions: ['utut_2525', 'utut_816', 'utut_baby'],
        keywords: ['ゆた', 'ユタ', 'utut'],
        fanMark: '🎀',
        color: '#06b6d4',
      },
      'にしき': {
        mentions: ['nishiki1225', 'nishiki1225_2'],
        keywords: ['にしき', 'ニシキ', 'nishiki'],
        fanMark: '❄',
        color: '#e5e7eb',
      },
    },
  },
  knightx: {
    name: 'Knight X - 騎士X -',
    fanMark: '⚔️',
    officialAccount: 'Info_KnightX',
    members: {
      'しゆん': {
        mentions: ['shiyun928', 'shiyun_2525', '4yn_i'],
        keywords: ['しゆん', 'シユン', 'shiyun', 'しゆりす'],
        fanMark: '🐿️',
        color: '#22c55e',
      },
      'ばぁう': {
        mentions: ['Vau1113', 'Vau0307'],
        keywords: ['ばぁう', 'バァウ', 'ばぁう民', 'vau'],
        fanMark: '🐰',
        color: '#ef4444',
      },
      'てるとくん': {
        mentions: ['teruto_nico', 'teruto_sub', 'teruto_nanoda'],
        keywords: ['てるとくん', 'てると', 'テルト', 'てるなー', 'teruto'],
        fanMark: '🎮',
        color: '#ec4899',
      },
      'タケヤキ翔': {
        mentions: ['takeyakii', 'takeyaki_uraura'],
        keywords: ['タケヤキ翔', 'タケヤキ', 'たけやき', 'takeyaki', 'ラトゥラトゥ'],
        fanMark: '🦅',
        color: '#e5e7eb',
      },
    },
  },
  meteora: {
    name: 'めておら - Meteorites -',
    fanMark: '💫',
    officialAccount: 'mtor_info',
    members: {
      '心音': {
        mentions: ['mtor_shion', 'shionsab'],
        keywords: ['心音', 'しおん', 'シオン', 'shion'],
        fanMark: '💫',
        fanEmojis: ['💫'],
        color: '#a855f7',
      },
      'ロゼ': {
        mentions: ['mtor_rose', 'Rosesamadazo'],
        keywords: ['ロゼ', 'ろぜ', 'roze', 'rose'],
        fanMark: '🥀',
        fanEmojis: ['🥀', '💫'],
        color: '#ef4444',
      },
      'Lapis': {
        mentions: ['mtor_lapis', 'Lapiskawaii'],
        keywords: ['Lapis', 'らぴす', 'ラピス', 'lapis'],
        fanMark: '💎',
        fanEmojis: ['💎', '💫'],
        color: '#06b6d4',
      },
      'メルト': {
        mentions: ['mtor_melt'],
        keywords: ['メルト', 'めると', 'melt', 'ダ・テンシ', 'テンシ'],
        fanMark: '👼',
        fanEmojis: ['👼', '💫'],
        color: '#e5e7eb',
      },
      'みかさ': {
        mentions: ['mtor_mikasa'],
        keywords: ['みかさ', 'ミカサ', 'mikasa'],
        fanMark: '🍳',
        fanEmojis: ['🍳', '💫'],
        color: '#ec4899',
      },
      'らいと': {
        mentions: ['mtor_light'],
        keywords: ['らいと', 'ライト', 'raito', 'light', '明雷'],
        fanMark: '⚡',
        fanEmojis: ['⚡', '💫'],
        color: '#eab308',
      },
    },
  },
}

// デフォルトグループ
export const DEFAULT_GROUP = 'amptakcolors'

// 後方互換: 現在選択中のグループのメンバーを取得するヘルパー
export function getOfficialAccounts(groupId) {
  return GROUPS[groupId]?.members || {}
}

// ハートの絵文字と色名
export const HEART_EMOJIS = {
  '❤️': { name: '赤', color: '#ef4444' },
  '🧡': { name: 'オレンジ', color: '#f97316' },
  '💛': { name: '黄色', color: '#eab308' },
  '💚': { name: '緑', color: '#22c55e' },
  '💙': { name: '青', color: '#3b82f6' },
  '💜': { name: '紫', color: '#a855f7' },
  '🖤': { name: '黒', color: '#1f2937' },
  '🤍': { name: '白', color: '#e5e7eb' },
  '🤎': { name: '茶', color: '#a16207' },
  '💗': { name: 'ピンク(成長)', color: '#ec4899' },
  '💖': { name: 'ピンク(輝)', color: '#f472b6' },
  '💕': { name: 'ダブルハート', color: '#f9a8d4' },
  '💞': { name: '回転ハート', color: '#fb7185' },
  '💓': { name: '鼓動', color: '#f43f5e' },
  '💘': { name: '矢ハート', color: '#be123c' },
  '💝': { name: 'リボンハート', color: '#fbbf24' },
}

/**
 * ユーザーが特定メンバーのファンかどうかを判定する共通関数
 * 検索対象: bio + name + quote_text(A) + location(F)
 * 判定基準: mentions / keywords / fanMark絵文字(B)
 * @returns {string[]} マッチしたメンバー名の配列
 */
export function detectFanOf(user, members) {
  const raw = getUserSearchText(user)
  const lower = raw.toLowerCase()

  const matched = []
  Object.entries(members).forEach(([name, data]) => {
    const hasMention = data.mentions.some(acc => lower.includes(`@${acc.toLowerCase()}`))
    const hasKeyword = data.keywords.some(kw => lower.includes(kw.toLowerCase()))
    const emojis = data.fanEmojis || (data.fanMark ? [data.fanMark] : [])
    const hasFanMark = emojis.some(emoji => raw.includes(emoji))
    if (hasMention || hasKeyword || hasFanMark) matched.push(name)
  })
  return matched
}

/**
 * bio+name+quote_text+location の結合文字列を取得（キャッシュ付き）
 */
export function getUserSearchText(user) {
  if (user._searchText !== undefined) return user._searchText
  user._searchText = [user.bio || '', user.name || '', user.quote_text || '', user.location || ''].join(' ')
  return user._searchText
}

/**
 * ユーザー配列に対して一括でファン判定+ハート判定+DM統計を1パスで実行。
 * user._fanCache[groupId] にファン結果をキャッシュして再利用。
 * @returns {{ mentionCounts, heartCounts, groupEmojiCounts, dmStats, fanMap }}
 */
export function computeStatsOnePass(users, members, groupId) {
  const heartEmojis = Object.keys(HEART_EMOJIS)

  // 結果バケット初期化
  const mentionCounts = {}
  Object.entries(members).forEach(([name]) => {
    mentionCounts[name] = { count: 0, users: [] }
  })

  const heartCounts = {}
  Object.entries(HEART_EMOJIS).forEach(([emoji, info]) => {
    heartCounts[emoji] = { ...info, emoji, count: 0, users: [] }
  })

  const groupEmojiCounts = {}
  Object.entries(members).forEach(([name, data]) => {
    if (!data.fanMark) return
    groupEmojiCounts[data.fanMark] = { name, emoji: data.fanMark, count: 0, users: [] }
  })

  const dmStats = { open: 0, closed: 0, unknown: 0 }

  // fanMap: username -> string[] (キャッシュ結果を返す)
  const fanMap = new Map()

  // === 1パスで全統計を計算 ===
  for (const user of users) {
    // ファン判定（キャッシュ利用）
    if (!user._fanCache) user._fanCache = {}
    let fans
    if (user._fanCache[groupId]) {
      fans = user._fanCache[groupId]
    } else {
      fans = detectFanOf(user, members)
      user._fanCache[groupId] = fans
    }
    fanMap.set(user.username, fans)

    for (const name of fans) {
      mentionCounts[name].count++
      mentionCounts[name].users.push(user.username)
    }

    // テキスト結合（キャッシュ付き）
    const bio = getUserSearchText(user)

    // ハート絵文字
    for (const emoji of heartEmojis) {
      if (bio.includes(emoji)) {
        heartCounts[emoji].count++
        heartCounts[emoji].users.push(user.username)
      }
    }

    // グループ絵文字
    for (const emoji in groupEmojiCounts) {
      if (bio.includes(emoji)) {
        groupEmojiCounts[emoji].count++
        groupEmojiCounts[emoji].users.push(user.username)
      }
    }

    // DM統計
    if (user.can_dm === true) dmStats.open++
    else if (user.can_dm === false) dmStats.closed++
    else dmStats.unknown++
  }

  return { mentionCounts, heartCounts, groupEmojiCounts, dmStats, fanMap }
}

export function parseFollowerCount(str) {
  if (!str) return 0
  const num = parseFloat(String(str).replace(/,/g, ''))
  if (String(str).includes('K')) return num * 1000
  if (String(str).includes('M')) return num * 1000000
  if (String(str).includes('B')) return num * 1000000000
  return num || 0
}

export function mergeUser(existing, incoming, fileName) {
  const merged = { ...existing }
  const fields = ['name', 'bio', 'verified', 'profile_image_url',
                  'location', 'url', 'followers_count', 'following_count', 'can_dm',
                  'statuses_count', 'favourites_count', 'media_count', 'listed_count',
                  'created_at', 'is_blue_verified', 'protected', 'default_profile_image',
                  'profile_banner_url', 'rest_id']
  for (const f of fields) {
    if (!merged[f] && incoming[f]) {
      merged[f] = incoming[f]
    }
  }
  if (incoming.quote_text) {
    if (merged.quote_text && merged.quote_text !== incoming.quote_text) {
      merged.quote_text = merged.quote_text + '\n---\n' + incoming.quote_text
    } else if (!merged.quote_text) {
      merged.quote_text = incoming.quote_text
    }
  }
  const sources = new Set(merged._sources || [])
  sources.add(fileName)
  merged._sources = [...sources]
  return merged
}
